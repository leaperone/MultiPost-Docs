---
title: Overview
---

## 原理介绍

开发者首先需要在 `multipost.app` 中创建一个账号，然后通过 `multipost.app` 的 API 接口，来实现扩展信息获取、发布任务创建等功能。

## 效果
1. 现在需要一个在图形系统（比如 Windows、Linux GUI、MacOs）上面长时间运行一个安装了扩展的浏览器。称之为 “维护 Node”。

2. 注册 MultiPost 账号，然后在网页端连接扩展，并登记到 MultiPost 后台。成为 Node

3. 你可以用 Restful 接口调用这个 Node 执行发布任务。

### 示例
1. 你在自己的电脑上面，维护了一个 Node，然后用 cron 定时任务，每天定时运行发布天气讯息。
2. 你在自己的电脑上面，维护了一个 Node，然后用 Python 脚本调用 Node 的 Restful 接口，来发布 Meme 图片。
3. 你在云电脑上面，维护了一个 Node，然后用服务器上的 Shell 脚本调用 Node 的 Restful 接口，来发布新闻资讯。

## 使用

1. 在 [multipost.app](https://multipost.app) 中创建一个账号
2. 安装 MultiPost 扩展
3. 打开 [Extension Client](https://multipost.app/dashboard/publish/client)
4. 点击 `Connect Extension` 按钮，连接扩展
5. 等待扩展第一次连接并获取 `Client ID`
6. 打开 [API Keys](https://multipost.app/dashboard/api-keys)
7. 点击 `Create API Key` 按钮，创建一个 API Key
8. 复制 `API Key`
9. 使用 `API Key` 以及 `Client ID` 调用相关接口

## 名词解析

### Node / Client 
Node （或者是 Client）是一个运行了 MultiPost 扩展的浏览器。需要长期运行，并有互联网连接到 MultiPost 服务器。
不能够白杀后台程序关闭，否则无法正常工作。

### API Key
API Key 是用于调用 Restful API 的凭证。
